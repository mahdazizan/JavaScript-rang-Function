# JavaScript-rang-Function
The range function is a useful function that produces arrays of consecutive numbers. This function is not available by default in JavaScript, but we can easily implement it.
